name: example
description: Showcase for material_async_button.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  material_async_button:
    path: ../

dev_dependencies:
  checks: ^0.3.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
